fn main() {
    mod math {
        type Complex = (f64, f64);
        fn sin(f: f64) -> f64 {
            /* ... */
    fail!();
        }
        fn cos(f: f64) -> f64 {
            /* ... */
    fail!();
        }
        fn tan(f: f64) -> f64 {
            /* ... */
    fail!();
        }
    }
}